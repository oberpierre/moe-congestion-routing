# Collapse Induction & Recovery
#
# Architecture is base_cluster.yaml untouched: 64 experts, top-8, softmax, 9 layers. Only the
# budget changes, because a router recovers on a step budget rather than a token budget.
extends: ../base_cluster.yaml

global_batch_size: 128

eval_interval: 500
save_interval: 250
