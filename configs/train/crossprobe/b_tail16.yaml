# Cell b_tail16: run B's step-500 checkpoint, tail instrument, all 16 sequences.
#
# moe_probe_dir is pinned for the same reason as the other cells: with `load` set, a derived probe
# dir would land in the source run's own probes/ and overwrite its iter_0000000.npz.
extends: _base.yaml

load: artifacts/exp1/alflb/20260824-232033/checkpoints
moe_probe_batch: assets/probe/standing_climbmix_small_16x2048.npz
moe_probe_seqs: 16
moe_probe_dir: artifacts/exp1/crossprobe/b_tail16/probes
