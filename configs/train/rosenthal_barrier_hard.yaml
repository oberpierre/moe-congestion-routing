extends: base_cluster.yaml

moe_router_load_balancing_type: rosenthal
moe_rosenthal_variant: hard
moe_rosenthal_cost: softplus_barrier
# moe_rosenthal_lambda omitted: inherits the barrier's slope-matched default (0.2 = 2*tau at
# tau=0.1) from DEFAULT_LAMBDA in src/moe_congestion_routing/losses/cost_families.py.
moe_aux_loss_coeff: 0.01

output_dir: artifacts/exp1/rosenthal_barrier_hard
